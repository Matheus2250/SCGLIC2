services:
  # Backend API
  - type: web
    name: sistema-contratacoes-api
    env: python
    region: oregon
    plan: free
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sistema-contratacoes-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ENVIRONMENT
        value: production

  # Frontend React
  - type: web
    name: sistema-contratacoes-web
    env: node
    region: oregon
    plan: free
    buildCommand: cd frontend && npm ci && npm run build
    startCommand: cd frontend && npx serve -s build -l $PORT
    envVars:
      - key: VITE_API_URL
        value: https://scglic.onrender.com

databases:
  # PostgreSQL Database
  - name: sistema-contratacoes-db
    databaseName: contratos_db
    user: contratos_user
    region: oregon
    plan: free